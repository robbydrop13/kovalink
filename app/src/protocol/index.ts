// Point d'import unique du protocole partagé.
// Branché sur le paquet réel `@kovalink/protocol` (packages/protocol),
// qui est la source unique de vérité entre le daemon et l'app.
export * from '@kovalink/protocol';
