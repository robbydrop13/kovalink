// Thème appliqué. Un seul jeu câblé en v1 : le sombre (A9).
import { dark, layout, motion, radius, space, type } from './tokens';

export const colors = dark;
export { layout, motion, radius, space, type };
export { MONO_FAMILY } from './tokens';
export type { TypeToken } from './tokens';
